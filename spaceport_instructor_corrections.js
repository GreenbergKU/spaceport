class Part {
    constructor(part) {
        this.part = part;
        this.name = part.name;
        this.validTypes = [
            'shell',
            'hyperdrive',
            'computer',
        ]
        //'new className' in test should transcribe to 'constructor("className.name")      
    }
}
    //1 line break between classes

    //curly brackets should line up with the beginning of if statement
        
    if ( Part /* your code here */ ) { 
    } else {
      // your code here
    };

    // if (something) {model.value || 10}

    
//inclass example from FE MOD1 "Intro to JS Testing (Part II)" 
//ru
class Being {
    constructor (name) {
        this.isAlive = true;
        this.name = name;
    }
}

class Part
    constructor(diffObject) {
        var validTypes = {
            'shell',
            'hyperdrive',
            'computer',
        }

//MANY syntax errors, most common outlined in class BELOW
    class Body {
        constructor(object) {
            this.variable = object.name;
        }
    }
        
        function () {
            code;
        };

     class Part {
        constructor(object) {
            this.name = object.name;
        }
    }

    class Ship

    //curly brackets should line up with the beginning of of statement
        if ( ) { 
        } else {
        };

       if( this.value ) =  model.value || 10;

